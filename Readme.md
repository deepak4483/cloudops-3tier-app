# cloudops-3tier-app

A minimal Express.js application with a root and health endpoint.

Quick start

1. Install dependencies

```powershell
npm install
```

2. Start server

```powershell
npm start
```

3. Open http://localhost:3000/ and http://localhost:3000/health
